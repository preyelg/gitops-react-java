replicaCount: 2

namespace: frontend

image:
  repository: preyelg/react-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: frontend.example.com
  path: /
